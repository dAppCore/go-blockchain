version: 1

project:
  name: go-blockchain
  description: Lethean blockchain node
  binary: core-chain

build:
  cgo: true
  entry: ./cmd/core-chain
  flags:
    - -trimpath
  ldflags:
    - -s
    - -w
  env:
    - GOWORK=off

targets:
  - os: linux
    arch: amd64

archive:
  format: tar.gz
